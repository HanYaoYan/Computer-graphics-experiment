# 部署说明文档

## 一、系统要求

### 1.1 硬件要求
- **处理器**：Intel Core i3 或同等性能的AMD处理器
- **内存**：至少 512MB 可用内存（推荐 1GB 或更多）
- **硬盘空间**：至少 50MB 可用空间
- **显示器**：分辨率至少 1024x768（推荐 1280x720 或更高）
- **输入设备**：鼠标和键盘

### 1.2 软件要求
- **操作系统**：
  - Windows 7/8/10/11（32位或64位）
  - Linux（Ubuntu 18.04+, Debian 10+, CentOS 7+等）
  - macOS 10.13 或更高版本
- **Python**：3.7 或更高版本（推荐 3.8+）
- **pip**：Python包管理器（通常随Python一起安装）

## 二、安装步骤

### 2.1 Windows系统部署

#### 步骤1：检查Python安装
1. 打开命令提示符（CMD）或PowerShell
2. 输入以下命令检查Python版本：
   ```bash
   python --version
   ```
3. 如果未安装Python，请访问 [python.org](https://www.python.org/downloads/) 下载并安装
   - 安装时勾选"Add Python to PATH"选项

#### 步骤2：获取项目文件
1. 将项目文件夹复制到本地目录，例如：`E:\temp\计算机图形学`
2. 打开命令提示符，切换到项目目录：
   ```bash
   cd E:\temp\计算机图形学
   ```

#### 步骤3：创建虚拟环境（推荐）
1. 创建虚拟环境：
   ```bash
   python -m venv venv
   ```
2. 激活虚拟环境：
   ```bash
   venv\Scripts\activate
   ```
   - 激活成功后，命令提示符前会显示`(venv)`

#### 步骤4：安装依赖
```bash
pip install -r requirements.txt
```

#### 步骤5：验证安装
```bash
python -c "import pygame; import numpy; print('安装成功！')"
```

如果显示"安装成功！"，说明环境配置正确。

#### 步骤6：运行程序
```bash
python main.py
```

---

### 2.2 Linux系统部署

#### 步骤1：检查Python安装
```bash
python3 --version
```

如果未安装，使用包管理器安装：
- Ubuntu/Debian:
  ```bash
  sudo apt update
  sudo apt install python3 python3-pip python3-venv
  ```
- CentOS/RHEL:
  ```bash
  sudo yum install python3 python3-pip
  ```

#### 步骤2：获取项目文件
```bash
cd /path/to/项目目录
```

#### 步骤3：创建虚拟环境
```bash
python3 -m venv venv
source venv/bin/activate
```

#### 步骤4：安装依赖
```bash
pip install -r requirements.txt
```

#### 步骤5：安装系统依赖（如果需要）
某些Linux发行版可能需要安装SDL库：
```bash
# Ubuntu/Debian
sudo apt install libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev

# CentOS/RHEL
sudo yum install SDL2-devel SDL2_image-devel SDL2_mixer-devel SDL2_ttf-devel
```

#### 步骤6：运行程序
```bash
python3 main.py
```

---

### 2.3 macOS系统部署

#### 步骤1：检查Python安装
```bash
python3 --version
```

macOS通常自带Python，如果没有或版本过低，可以：
- 使用Homebrew安装：
  ```bash
  brew install python3
  ```

#### 步骤2：获取项目文件
```bash
cd /path/to/项目目录
```

#### 步骤3：创建虚拟环境
```bash
python3 -m venv venv
source venv/bin/activate
```

#### 步骤4：安装依赖
```bash
pip install -r requirements.txt
```

#### 步骤5：运行程序
```bash
python3 main.py
```

---

## 三、快速部署（不使用虚拟环境）

如果不想使用虚拟环境，可以直接在系统Python环境中安装：

### Windows:
```bash
pip install pygame==2.5.2 numpy==1.24.3
python main.py
```

### Linux/macOS:
```bash
pip3 install pygame==2.5.2 numpy==1.24.3
python3 main.py
```

**注意**：直接安装可能会与系统其他Python项目产生依赖冲突，建议使用虚拟环境。

## 四、依赖包说明

### 4.1 必需依赖
- **pygame (2.5.2)**：图形界面和绘图库
  - 用途：提供窗口管理、事件处理、图形绘制功能
  - 大小：约 10-15MB

- **numpy (1.24.3)**：数值计算库
  - 用途：矩阵运算，用于几何变换
  - 大小：约 20-30MB

### 4.2 依赖安装问题排查

#### 问题1：pip安装速度慢
**解决方案**：
使用国内镜像源：
```bash
# 使用清华镜像
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# 或使用阿里云镜像
pip install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt
```

#### 问题2：pygame安装失败（Windows）
**解决方案**：
1. 确保已安装Microsoft Visual C++ Redistributable
2. 尝试使用预编译的wheel文件：
   ```bash
   pip install --only-binary :all: pygame
   ```

#### 问题3：pygame安装失败（Linux）
**解决方案**：
安装SDL开发库：
```bash
sudo apt install python3-dev libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev
```

## 五、程序运行

### 5.1 启动程序
在项目目录下运行：
```bash
python main.py
```
或（Linux/macOS）：
```bash
python3 main.py
```

### 5.2 程序界面
- 启动后会出现一个 1000x700 像素的窗口
- 左侧 800x600 区域为绘图画布
- 右侧 200x700 区域为工具栏

### 5.3 退出程序
- 点击窗口关闭按钮
- 或按 Alt+F4（Windows/Linux）
- 或按 Cmd+Q（macOS）

## 六、文件结构说明

部署后的项目目录结构：
```
计算机图形学/
├── main.py                 # 主程序（运行此文件）
├── graphics_system.py       # 图形系统核心
├── graphics_primitives.py   # 图形生成模块
├── clipping.py             # 裁剪模块
├── transform.py            # 变换模块
├── fill.py                 # 填充模块
├── command_system.py        # 命令系统
├── file_format.py          # 文件格式处理
├── requirements.txt        # 依赖列表
├── README.md               # 项目说明
├── 测试说明.md             # 测试文档
├── 部署说明.md             # 本文档
├── graphics_save.json      # 保存的图形文件（运行后生成）
└── venv/                   # 虚拟环境目录（如果使用）
```

## 七、常见问题解决

### 7.1 程序无法启动

**问题**：运行`python main.py`后没有反应或报错

**排查步骤**：
1. 检查Python版本：
   ```bash
   python --version
   ```
   确保版本 >= 3.7

2. 检查依赖是否安装：
   ```bash
   python -c "import pygame; import numpy"
   ```
   如果报错，重新安装依赖

3. 检查文件完整性：
   确保所有.py文件都在项目目录中

### 7.2 窗口无法显示

**问题**：程序运行但窗口不显示

**解决方案**：
1. 检查显示器设置
2. 尝试全屏模式（需要修改代码）
3. 检查是否有其他程序占用显示

### 7.3 绘图功能异常

**问题**：点击工具但无法绘图

**解决方案**：
1. 确保鼠标在画布区域内点击
2. 检查工具是否正确选择（工具栏中工具应高亮显示）
3. 对于曲线工具，需要点击4个点

### 7.4 文件保存/加载失败

**问题**：保存或加载文件时出错

**解决方案**：
1. 检查文件权限（确保有写入权限）
2. 检查磁盘空间
3. 检查文件路径是否正确
4. 确保JSON文件格式正确（如果手动编辑过）

### 7.5 性能问题

**问题**：程序运行缓慢或卡顿

**解决方案**：
1. 关闭其他占用资源的程序
2. 减少画布上的图形对象数量
3. 检查系统内存使用情况

## 八、卸载说明

### 8.1 卸载程序
直接删除项目文件夹即可。

### 8.2 卸载依赖（如果使用虚拟环境）
```bash
# 删除虚拟环境目录
rm -rf venv  # Linux/macOS
rmdir /s venv  # Windows
```

### 8.3 卸载依赖（如果直接安装到系统）
```bash
pip uninstall pygame numpy
```

## 九、更新说明

### 9.1 更新程序
1. 备份当前项目文件夹
2. 用新版本文件替换旧文件
3. 检查requirements.txt是否有更新
4. 如有新依赖，运行：
   ```bash
   pip install -r requirements.txt --upgrade
   ```

### 9.2 更新依赖
```bash
pip install --upgrade pygame numpy
```

## 十、技术支持

如遇到部署问题，请检查：
1. Python版本是否符合要求
2. 依赖包是否正确安装
3. 文件是否完整
4. 系统权限是否足够

## 十一、部署检查清单

部署前请确认：
- [ ] Python 3.7+ 已安装
- [ ] pip 可用
- [ ] 项目文件完整
- [ ] 有足够的磁盘空间
- [ ] 有网络连接（用于下载依赖）

部署后请验证：
- [ ] 程序可以正常启动
- [ ] 窗口正常显示
- [ ] 可以绘制图形
- [ ] 可以保存和加载文件
- [ ] 撤销/重做功能正常

4. 
